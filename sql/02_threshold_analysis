-- ==========================================
-- SEGMENT ANALYSIS
-- =========================================
-- Validating the policy count at both groups to verify if the counts are in an acceptable range

SELECT
    age_band,
    tenure_band,
    COUNT(*) AS policy_count
FROM fact_premium
WHERE stable_pricing_flag = 1
GROUP BY age_band, tenure_band
ORDER BY policy_count ASC;
